// Generated file. Do not edit.

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

apply plugin: "com.android.library"
apply plugin: "kotlin-android"
apply plugin: 'maven-publish'
android {
    namespace = "com.myphsar.myphsar_module"
    compileSdk = 35
    defaultConfig {
        minSdk = 21
        targetSdk = 35
    }
}

//publishing {
//    publications {
//        release(MavenPublication) {
//            groupId = 'com.myphsar.myphsar_module' // Replace with your desired group ID
//            artifactId = 'flutter_release' // Replace with your desired artifact ID
//            version = '1.0.0' // Uses the version from pubspec.yaml
//
//            afterEvaluate {
//                if (project.tasks.findByName("bundleReleaseAar")) {
//                    artifact bundleReleaseAar
//                }
//            }
//        }
//    }
//    repositories {
//        maven {
//            url = uri("/Users/sak/Documents/myphsar_module/build/host/outputs/repo")
//        }
//    }
//}


// Add these plugins

//
//
//afterEvaluate {
//    publishing {
//        publications {
//            release(MavenPublication) {
//                from components.release
//
//                groupId = 'io.github.sak-coder'
//                artifactId = 'flutter_release'
//                version = '1.0.0'
//
//                pom {
//                    name = 'myphsar'
//                    description = 'A Flutter module for Myphsar'
//                    url = 'https://github.com/sak-coder/mps'
//
//                    licenses {
//                        license {
//                            name = 'Apache-2.0'
//                            url = 'https://opensource.org/licenses/Apache-2.0'
//                        }
//                    }
//
//                    developers {
//                        developer {
//                            id = 'sak'
//                            name = 'mps'
//                            email = 'contact@myphsar.com'
//                        }
//                    }
//
//                }
//            }
//        }
//
//        repositories {
//            maven {
//
//                name = "GitHubPackages"
//                url = uri("https://maven.pkg.github.com/sak-coder/io/github/sak-coder")
//
//                credentials {
//                   username = project.properties["sak-coder"] ?: ""
//                    password = project.properties['github$pwd$93'] ?: ""
//                }
//            }
//        }
//    }
//
//
//}



afterEvaluate {
    publishing {
        publications {
            release(MavenPublication) {
                // Get a reference to the release Android component
                from components.release

                // Use the POM file generated by the flutter build command
                pom.withXml {
                    def dependenciesNode = asNode().appendNode('dependencies')
                    // Loop through all generated POM files and merge dependencies
                    project.configurations.api.allDependencies.each {
                        if (it.group != null && (it.name != null && it.version != null)) {
                            def dependencyNode = dependenciesNode.appendNode('dependency')
                            dependencyNode.appendNode('groupId', it.group)
                            dependencyNode.appendNode('artifactId', it.name)
                            dependencyNode.appendNode('version', it.version)
                        }
                    }
                }
                // Define the Maven coordinates (groupId, artifactId, version)
                groupId = 'io.github.sak-coder'
                artifactId = 'flutter_release'
                version = '1.0.0' // Use the same version as --build-number
            }
        }
        repositories {
            // Configure your remote Maven repository here
            maven {
                url = uri("https://github.com/sak-coder/mps") // Replace with your repository URL
                credentials {
                    username = project.properties["sak-coder"] ?: ""
                    password = project.properties['github$pwd$93'] ?: ""
                }
            }
        }
    }
}
